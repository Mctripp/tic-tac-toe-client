'use strict'

// determine which mark to use
const getMarkType = () => {
  
} // getMarkType

// mark clicked grid with player's mark, disable event on square
const markGrid = () => {

} //markGrid

module.exports = {
  markGrid
}
